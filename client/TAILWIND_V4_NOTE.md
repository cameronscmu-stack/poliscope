# Tailwind v4 Note

This project uses Tailwind CSS v4 with the @tailwindcss/vite plugin.

CSS entry: Use `@import "tailwindcss";` (NOT `@tailwind base/components/utilities;`)
Config: tailwind.config.js works for theme extensions with `@config` directive.
